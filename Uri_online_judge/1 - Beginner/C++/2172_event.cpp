#include <bits/stdc++.h>
using namespace std;

int main ()
{
   
	while (1)
	{
		long long int x, m, xp;
		cin >> x >> m;
		
		if (x == 0)
			break;
		else
			xp = x * m;
	
		cout << xp << "\n";		
	}

	
	return 0;
}
