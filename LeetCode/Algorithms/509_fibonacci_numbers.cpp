#include <bits/stdc++.h>
using namespace std;

 int fib(int N) {
      
      /* recursive to much slow */
      if(N < 2) return N;
      else {
        return fib(N-1) + fib(N - 2);
        
      return sum;
 }  


int main() {
  
  cout << fib(10) << endl;
  return 0;
}
